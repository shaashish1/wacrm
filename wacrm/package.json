{
  "name": "wacrm-monorepo",
  "version": "0.8.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=@wacrm/shared && npm run build --workspace=@wacrm/web && npm run build --workspace=@wacrm/worker",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build --workspace=@wacrm/shared && npm run typecheck --workspace=@wacrm/web && npm run typecheck --workspace=@wacrm/worker",
    "test": "npm run test --workspaces --if-present"
  }
}
